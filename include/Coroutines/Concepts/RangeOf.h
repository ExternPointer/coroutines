#pragma once

#include <concepts>
#include <ranges>
/*
namespace Coroutines::Concepts {
template<class T, class V>
concept CRangeOf = std::ranges::range<T> && std::is_same_v<V, std::ranges::range_value_t<T>>;
}
*/